# 简历表述与证据映射

本仓库需要同时判断两个维度：

- 工程构建状态：代码、测试和合成产物是否真实存在且可复现；
- 业务声明状态：真实授权、模型效果、编辑效率、发布、用户和收入是否有证据。

当前工程构建状态为 `PASS`，业务声明状态为 `DEMO_ONLY`。因此可以写“独立构建并跑通合成作品集”，不能写成真实公司或客户项目成果。

## 现在可以核验的工程声明

| 声明 | 证据 |
|---|---|
| 独立构建端到端小说 AI 数据管道 | `src/`、`tests/`、`architecture/` |
| 实现权利字段硬门、规范化、精确/近似去重 | `validation.py`、`dedup.py`、负向测试 |
| 实现作者/作品级切分与评测隔离 | `splitting.py`、`dataset_isolation_report.json` |
| 生成 SFT、偏好和隔离评测三类合成数据 | `artifacts/datasets/`、`contracts/` |
| 维护数据、模型占位版本、评测和源码血缘 | `release_manifest.json`、`version_lineage.json` |
| 17 项自动化测试和公开发布安全门通过 | `tests/`、`scripts/check_public_repo.py` |

## 现在不能核验的业务声明

| 暂不可写内容 | 未来最低证据 |
|---|---|
| 处理 X 篇真实授权作品 | 权利台账、合同引用、冻结数据 Manifest、复核记录 |
| 模型盲测胜率达到 X% | 实际模型、冻结评测集、随机化和原始盲审标签 |
| 编辑耗时下降 X% | 真实工作台事件、同口径基线、统计周期与样本量 |
| 每月发布 X 篇或拥有 X 名用户 | 平台/系统记录、去重口径、脱敏核验副本 |
| 产生 X 元收入 | 合同/订单、到账、退款、平台费和财务复核 |

## 当前可直接使用的简历表述

> 独立设计并实现合成小说 AI 数据治理管道，覆盖权利字段门禁、SHA-256 与 MinHash/LSH 去重、作者/作品级隔离切分、SFT/偏好/评测集生产及版本血缘；通过 17 项自动化测试与公开发布安全检查。

如需写合成回归数字，必须保留限定语：

> 在确定性合成 Demo 上处理 27 条原始记录，产出 20 个唯一作品、36/12 条 SFT 训练/验证样本、24 组偏好对和 30 条隔离评测任务，作者/作品/正文哈希跨集合重叠均为 0。

不要写：

> 为朋友或公司训练出爆款小说模型，显著降低编辑成本并提高收入。

除非每个主体、模型运行、对照组、统计口径和经营结果都已有真实且可核验证据。
