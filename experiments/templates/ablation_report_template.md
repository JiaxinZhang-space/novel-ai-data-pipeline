# 消融实验报告

> 状态：`TEMPLATE_NOT_RUN`  
> 消融用于判断组件贡献，不等同于证明生产收益。

## 1. 实验目标

- 完整系统版本：
- 被检验组件：
- 预期影响维度：
- 假设：

## 2. 变量控制

除“被移除或替换的组件”外，以下条件必须保持一致：

- 数据输入范围和版本：
- 基础模型和模型制品：
- 提示模板：
- 训练预算或推理采样参数：
- 评测集和量表：
- 随机种子：

## 3. 变体定义

| 变体 ID | 与完整系统的唯一差异 | 预期风险 |
|---|---|---|
| `full` | 无 | 基准 |
| `<VARIANT_ID>` | `<REMOVED_OR_REPLACED_COMPONENT>` | `<EXPECTED_RISK>` |

## 4. 结果

| 变体 | 任务数 | 平均分 | 通过率 | 盲测胜率 | 相对完整系统变化 |
|---|---:|---:|---:|---:|---:|
| `full` | `<NULL>` | `<NULL>` | `<NULL>` | `<NULL>` | `0` |
| `<VARIANT_ID>` | `<NULL>` | `<NULL>` | `<NULL>` | `<NULL>` | `<NULL>` |

## 5. 分层分析

- 受影响最大的任务类型：
- 未受影响的任务类型：
- 是否存在作者、题材或长度偏差：
- 是否存在成本或延迟变化：

## 6. 解释边界

- 可以支持的结论：
- 样本量不足以支持的结论：
- 与其他组件的交互：
- 需要重复验证的条件：

## 7. 决策

- 保留、修改或移除组件：
- 下一实验：
- 风险与回滚方案：

## 8. 可复核引用

- 实验 Manifest：
- 结果文件及 SHA-256：
- 代码提交：
- 复核人和复核时间：

